mkdir ../smartycompile
mkdir ../smartycompile/templates_c
chmod -R 777 ../smartycompile
cat .htaccess.user | sed "s/<username>/`whoami`/g" > .htaccess
cat config.user.ini | sed "s/<username>/`whoami`/g" > config.ini
ln -s ../intranet2 ../web-docs/i2
